<%= form_for(@user_work_category) do |f| %>
  <% if @user_work_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_work_category.errors.count, "error") %> prohibited this user_work_category from being saved:</h2>

      <ul>
      <% @user_work_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :name_id %><br>
    <%= f.number_field :name_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
